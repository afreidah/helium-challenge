# ------------------------------------------------------------------------------
# Terraform core
# ------------------------------------------------------------------------------
# Local state
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Module/plugin directories
.terraform/
.terraform/**

# Variable files (keep examples)
*.tfvars
*.tfvars.json
!*.example.tfvars
!*.tfvars.example

# Generated/override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Plans (binary & JSON)
*.tfplan
plan.tfplan
plan.json

# Keep dependency lockfile tracked (recommended)
# (do NOT ignore .terraform.lock.hcl)

# ------------------------------------------------------------------------------
# Terragrunt
# ------------------------------------------------------------------------------
.terragrunt-cache/
**/.terragrunt-cache/
**/.terragrunt-source-version

# Optional debug vars Terragrunt can emit
debug.tfvars
**/debug.tfvars

# ------------------------------------------------------------------------------
# Security scanners (Checkov / Trivy) artifacts
# ------------------------------------------------------------------------------
# Per-run logs and temp outputs
.checkov/
**/.checkov/
**/plan-*.log

# If you generate per-dir configs via terragrunt generate hooks, ignore them:
**/.checkov.yaml
**/.trivy.yaml
**/.trivyignore

# ------------------------------------------------------------------------------
# Generic OS/editor noise
# ------------------------------------------------------------------------------
# macOS
.DS_Store

# Linux
*~

# Vim / Neovim
*.swp
*.swo
*.swn

# VS Code / JetBrains
.vscode/
.idea/

# Logs
*.log

# Node / Python (in case aux tooling sneaks in)
node_modules/
__pycache__/
*.pyc

